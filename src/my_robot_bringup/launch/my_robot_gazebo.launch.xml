<launch>
    <let name="urdf_path" 
         value="$(find-pkg-share my_robot_description)/urdf/my_robot.urdf.xacro" />

    <let name="rviz_config_path"
        value="$(find-pkg-share my_robot_description)/rviz/urdf_config.rviz" />

    <let name="gazebo_config_path"
        value="$(find-pkg-share my_robot_bringup)/config/gazebo_bridge.yaml" />

    <let name="controllers_config_path"
        value="$(find-pkg-share my_robot_bringup)/config/my_controllers.yaml" />

    <let name="ekf_config_path"
        value="$(find-pkg-share my_robot_bringup)/config/ekf.yaml" />

    <let name="ekf_imu_only_config_path"
        value="$(find-pkg-share my_robot_bringup)/config/ekf_imu_only.yaml" />

    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name="robot_description"
            value="$(command 'xacro $(var urdf_path) controllers_config:=$(var controllers_config_path)')" />
        <param name="use_sim_time" value="true" />
    </node>

    <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py" >
        <arg name="gz_args" value="$(find-pkg-share my_robot_bringup)/worlds/test_world.sdf -r" />
        <!--<arg name="gz_args" value="empty.sdf -r" />-->
    </include>

    <node pkg="controller_manager" exec="spawner"
      args="imu_sensor_broadcaster
            --controller-manager /controller_manager
            --controller-manager-timeout 60" />

    <node pkg="ros_gz_sim" exec="create" args="-topic robot_description" />

    <node pkg="ros_gz_bridge" exec="parameter_bridge">
        <param name="config_file" value="$(var gazebo_config_path)" />
    </node>

    <!-- Spawn ros2_control controllers (gz_ros2_control starts controller_manager) -->
    <node pkg="controller_manager" exec="spawner"
          args="joint_state_broadcaster
                --controller-manager /controller_manager
                --controller-manager-timeout 60" />

    <node pkg="controller_manager" exec="spawner"
          args="diff_drive_controller
                --controller-manager /controller_manager
                --controller-manager-timeout 60" />

    <!-- Extended Kalman Filter: fuses /diff_drive_controller/odom + /imu -->
    <node pkg="robot_localization" exec="ekf_node" name="ekf_filter_node" output="screen">
        <param name="use_sim_time" value="true" />
        <param from="$(var ekf_config_path)" />
    </node>

    <!-- IMU-only EKF: no wheel odometry – shows rapid drift from double integration -->
    <node pkg="robot_localization" exec="ekf_node" name="ekf_imu_only_node" output="screen">
        <param name="use_sim_time" value="true" />
        <param from="$(var ekf_imu_only_config_path)" />
        <remap from="odometry/filtered" to="odometry/imu_only" />
    </node>

    <node pkg="rviz2" exec="rviz2" output="screen" 
        args="-d $(var rviz_config_path)"/>
</launch>